<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- Example log pattern including 'callerMethod' + 'callerLine' MDC parameters for Unit Tests-->
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36}:%X{callerMethod}#%X{callerLine} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="TRACE">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>